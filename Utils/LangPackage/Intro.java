package Utils.LangPackage;

public class Intro {

    
    // we don't need to explicitly java.lang class to use it's functions, classes, interfaces 

    // but 
    
    // when we need to use the functions, classes, interface of the java.util then we have to explicitly import the class to use it 
    // java.util contains the who collection framwork of java 
    // scanner class , etc
}
